# CLAUDE.md — プロジェクト指示書

## プロジェクト概要

- **プロジェクト名**：prompt_editor
- **目的**：スペック駆動開発用のMarkdownファイル群を生成するプロンプトエディタ
- **担当者**：ihira
- **実装言語**：C++ / MFC（VS2022）

---

## Claudeへの基本指示

- 応答・コメントは**日本語**で統一すること
- コードのコメントも日本語で記述すること
- 不明点があれば作業前に必ず確認すること
- 破壊的変更（ファイル削除・上書き）は事前に報告し、承認を得てから実行すること
- タスク完了時は `tasks/tasklist.md` の該当項目をチェック済みに更新すること

### MFC作業時の行動規約（重要）

- **コードを提案・編集する前に、必ず該当の `.rc`（`prompt_editor.rc`）と `resource.h` を読むこと**。コントロールIDを推測で書かない
- ダイアログクラスを編集するときは、対応するヘッダ（メンバ変数とDDX定義）も併せて確認すること
- 新規のリソースID（`IDC_` / `IDD_` / `IDR_`）を勝手に発番せず、既存IDを使うこと。新規発番が必要な場合は理由とともに確認を求めること
- メッセージマップ（`BEGIN_MESSAGE_MAP`〜`END_MESSAGE_MAP`）は既存ブロックを確認してから追記し、マクロの省略・重複をしないこと
- ビルド構成・プロジェクト設定（プラットフォーム、文字セット、MFCリンク方式）を変更しないこと
- ビルドはClaudeが実行せず、VS2022側で行う前提とする（エラーが出たらテキストを貼って共有する運用）

### 実行時不具合（クラッシュ・誤動作）のデバッグ手順

- 実行時の不具合は、まずVS2022を中断させたデバッグ画面を提示する
- そこからClaude Codeが必要なデバッグ情報（呼び出し履歴・ローカル変数等）を要求してくるので、それに応じて提供する
- 呼び出し履歴（コールスタック）の取得：デバッグで中断した状態で `Ctrl+Alt+C` → ウィンドウ内で右クリック → 「すべてコピー」でテキストとして渡す
- コールスタックを正確に出すため、不具合追跡時は `Debug / x64` 構成でビルドすること（Release構成だと関数名が省略されることがある）

---

## ビルド環境・前提（変更不可）

| 項目 | 設定 |
|------|------|
| IDE / ビルド | VS2022 + MSBuild（Dev Container外でビルド） |
| MFCリンク方式 | 共有DLL（static MFCではない） |
| 文字セット | Unicode（`_UNICODE` / `UNICODE` 定義済み）。`CString` は実質 `CStringW` |
| ターゲットアーキテクチャ | x64（開発機はOmniBook X / ARM64だが、生成物はx64ターゲット。x64エミュレーション環境でビルドする） |
| C++標準 | C++17 |
| 作業フォルダ | `C:\work\claude\prompt_editor`（Dev Containerに `/workspaces/prompt_editor` としてbind mount） |

---

## MFCコーディング規約

- 文字列リテラルは `_T("...")` または `L"..."` で統一する。生の `"..."` は使わない
- DDX/DDVの向きに注意する
  - `UpdateData(TRUE)`：画面 → 変数（取得）
  - `UpdateData(FALSE)`：変数 → 画面（設定）
- リソースID命名規則を守る：`IDD_`（ダイアログ）／`IDC_`（コントロール）／`IDR_`（リソース）
- `new` した `CWnd` 派生オブジェクトの破棄責任・親子ウィンドウの所有権を明示的に管理する
- エラー処理の方針はプロジェクト内で統一する（MFC例外 `CException` 派生 / 標準例外のどちらかに寄せる）
- ファイル出力はUTF-8（BOMの有無は `MdGenerator` の既存実装に合わせる）

> 注：`.rc`・`resource.h`・各ダイアログクラスの「実体」はこのファイルに転記しない（古くなると害になるため）。
> 上記の行動規約に従い、作業のたびに実ファイルを参照すること。

---

## ディレクトリ構成

```
prompt_editor/
├── CLAUDE.md                    # この指示ファイル
├── .steering/
│   ├── product.md               # プロダクト要件・ゴール定義
│   ├── structure.md             # アーキテクチャ・技術スタック
│   └── decisions.md             # 設計判断の記録（ADR）
├── tasks/
│   └── tasklist.md              # タスク一覧・進捗管理
├── docs/                        # 設計書・仕様書・メモ
├── src/
│   ├── prompt_editor.rc         # ダイアログリソース定義
│   ├── resource.h               # リソースID定義
│   ├── PromptEditorApp.h/.cpp   # MFCアプリケーションクラス
│   ├── PromptEditorDlg.h/.cpp   # メインダイアログ
│   └── MdGenerator.h/.cpp       # Markdownファイル生成ロジック
└── prompt_editor.sln            # VS2022ソリューション
```

---

## 開発フロー（スペック駆動）

```
1. 要件定義   → .steering/product.md を更新
2. 設計       → .steering/structure.md を更新
3. タスク分解  → tasks/tasklist.md にタスクを追加
4. 実装       → タスクを上から順に実施（MFC作業時は上記の行動規約を厳守）
5. 確認       → タスクをチェック済みに更新
6. ビルド検証  → VS2022でビルド・動作確認、エラーはテキストで共有
```
